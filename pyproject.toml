[project]
name = "keyol-router"
version = "0.1.0"
description = "Deterministic conversational router for Keyol presence decisions."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
